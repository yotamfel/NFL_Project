# Copy this file to .env and adjust for your environment.
# Local Postgres needs no password here — libpq falls back to pgpass,
# same as the ETL scripts. A deployed host (e.g. Render) will instead
# provide a full connection string with embedded credentials.
DATABASE_URL=postgresql+psycopg2://postgres@localhost:5432/NFL_project
